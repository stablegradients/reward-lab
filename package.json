{
  "name": "rho",
  "version": "1.0.0",
  "private": true,
  "description": "A finite categorical visualizer of reinforcement learning updates, with paper-based algorithm explanations.",
  "license": "MIT",
  "scripts": {
    "dev": "python3 server.py",
    "build": "node scripts/build.cjs",
    "test": "node verify.js && node verify-site.js && node verify-classics.js && node verify-discovery.js && node verify-outputs.js && node verify-transfer.js && node verify-curves.js",
    "test:browser": "node qa-visualizer.cjs"
  },
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
